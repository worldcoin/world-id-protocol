# ============================================================
# World ID Bridge SDK — Deployment Environment
# ============================================================
# Copy to `.env` and fill in values.
#
# Only these env vars are needed — everything else is read
# from the declarative config at script/config/{env}.json.

# ─── Required ────────────────────────────────────────────────
# Deployer private key (must be funded on all target chains).
# PRIVATE_KEY=$(op read "op://Vault/Item/private-key")
PRIVATE_KEY=""

# ─── RPC Resolution ─────────────────────────────────────────
# Used to resolve per-chain RPCs via the alchemySlug field in config JSON.
# Not required if every chain has an explicit "rpc" field instead.
ALCHEMY_API_KEY=""

# ─── Optional ────────────────────────────────────────────────
# For `just verify` (block explorer contract verification).
ETHERSCAN_API_KEY=""

# Filter which destination networks to deploy (comma-separated).
# Omit to deploy all networks listed in config.
# DEPLOY_CHAINS=ethereum,base
