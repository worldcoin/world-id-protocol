# Mobile ZK baselines

Status: **placeholder** — no BrowserStack results committed yet.

Populate by running **Actions → Mobile Benchmarks** on `main` with **`open_baselines_pr=true`** (leave `pr_number` empty). On success the workflow opens a PR that updates this file plus `ios/summary.json` and `android/summary.json`; merge that PR to adopt the numbers.

See [README.md](README.md) for refresh policy.
