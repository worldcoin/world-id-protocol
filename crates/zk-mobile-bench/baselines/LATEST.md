# Mobile ZK baselines

Status: **placeholder** — no BrowserStack results committed yet.

Populate by running **Actions → Mobile Benchmarks** on `main` with `pr_number` left empty. That run rewrites this file plus `ios/summary.json` and `android/summary.json`.

See [README.md](README.md) for refresh policy and how PR check-runs use these files.
