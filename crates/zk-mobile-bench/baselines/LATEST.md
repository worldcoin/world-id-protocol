# Mobile ZK baselines

Status: **placeholder** — no BrowserStack results committed yet.

Populate by running **Actions → Mobile Benchmarks** on `main` with `pr_number` left empty. On success the workflow opens a PR that updates this file plus `ios/summary.json` and `android/summary.json`; merge that PR to adopt the numbers.

Until `meta.status` is `ready`, PR check-runs are descriptive only (no regression comparison).

See [README.md](README.md) for refresh policy.
