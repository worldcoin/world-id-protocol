{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "redirects": [
    {
      "source": "/",
      "destination": "/world_id_core/index.html"
    }
  ],
  "buildCommand": "$HOME/.cargo/bin/cargo doc -p world-id-core --no-deps --features authenticator,issuer,rp",
  "installCommand": "CARGO_HOME=\"$HOME/.cargo\" RUSTUP_HOME=\"$HOME/.rustup\" && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile default --no-modify-path",
  "outputDirectory": "target/doc"
}
