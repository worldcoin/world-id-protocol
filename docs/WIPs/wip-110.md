---
wip: 110
title: Flamingo Verifier
description: A TEE-backed service that compares images or embeddings under a requested assurance level and signs a token committing to its inputs.
author: Kilian Glas (@kilianglas), Paolo D'Amico (@paolodamico)
status: 🚧 Draft
type: Informational
created: 2026-08-03
---

> [!NOTE]  
> This spec outlines a draft functionality for beta testing. Changes are expected.

## 1. Abstract

The **Flamingo Verifier** (**Verifier**) is a TEE service that:

- receives up to eight entries (images or embeddings, each with opaque metadata), up to four of them to compare, a `method` and an `assurance_level`;
- hands them to a sandboxed biometric Engine that checks and compares them;
- when the Engine accepts, signs a token over the request hash, the method, the assurance level and the compared entries.

The token is its only signed output. [WIP-111][wip-111] consumes it to prove the comparison to a Relying Party without revealing any entry.

## 2. Motivation

- Comparing two embeddings is cheap. Extracting one anyone should believe is not.
- Extraction runs a model over raw media, which is impractical in a ZK circuit. Quality checks, Presentation Attack Detection and anti-fraud signals are worse, and may rely on model obscurity.
- An attested TEE keeps the inputs private and the execution verifiable. Committing to the raw inputs makes the result usable by a party that trusts neither the prover nor its device.

## 3. Specification

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in [RFC 2119](https://www.ietf.org/rfc/rfc2119.txt).

### 3.1 Request

The request MUST be encrypted to the Verifier. Its plaintext is a deterministic CBOR map:

| Field | Type | Bounds | Meaning |
| --- | --- | --- | --- |
| `method` | `uint` | `< 2^16` | Engine-defined algorithm, e.g. DeepFace with LightGuard. |
| `assurance_level` | `uint` | `< 2^8` | Engine-defined strictness within `method`. |
| `meta` | bytes | ≤ 1 KiB | Engine hints that do not change what is proven, e.g. experiment flags. |
| `entries` | array of `Entry` | `1..=MAX_ENTRIES` | Inputs to the Engine. |
| `compare` | array of `uint` | `2..=MAX_COMPARED` distinct indices into `entries` | Ordered; the Engine compares every pair. |

- An `Entry` is `{ data: bytes, meta: bytes }`: `data` is an encoded image or an embedding, `meta` (≤ 256 bytes) tells the Engine how to read it.
- The Verifier interprets none of these fields; it forwards them to the Engine ([§3.3](#33-engine)).
- Roles (Credential item, live frame, challenge) are fixed by `method` through positions in `compare`.
- The Verifier MUST reject a request of any other shape, exceeding a bound, or whose `compare` repeats or names an out-of-range index. It MAY impose further byte limits on `entries[].data`.

| Constant | Value | Description |
| --- | --- | --- |
| `MAX_ENTRIES` | `8` | Entries per request. |
| `MAX_COMPARED` | `4` | Compared entries, fixed by the token layout; at most six comparisons. |

### 3.2 Response

| Variant | Contents |
| --- | --- |
| `Success` | `token` ([§3.4](#34-token)), `signing_key_attestation`, `debug_report` |
| `Failed` | `reason`, `debug_report` |

- The response MUST be encrypted to the requester and SHOULD be padded to a fixed length, hiding the outcome and the report size.
- `reason` is one of `malformed_inputs`, `input_too_large`, `rejected(validation_reason)`, `no_match`, `internal`. `validation_reason` is the Engine's code, passed through.
- `debug_report` is optional bytes from the Engine, not part of the token. The Verifier MAY truncate it to fit the padded length.
- The Verifier MUST emit a token only when the Engine returns `Pass`.

### 3.3 Engine

The Engine performs extraction, quality checks, Presentation Attack Detection and comparison. This spec defines none of them.

- Each `method` defines the entry layout, how each entry's `meta` is read, and which checks run.
- Each `assurance_level` of a method defines the thresholds for every compared pair.
- Within one method, a higher `assurance_level` MUST be at least as strict on every check. Levels of different methods are not comparable.
- The Engine and its method and level definitions MUST be covered by the Verifier's attested measurement ([§3.5](#35-verifiability-and-reproducible-builds)), directly or through an attested hash of injected models and configuration.

The Engine interface is non-normative. The reference implementation uses:

| Engine request | Type |
| --- | --- |
| `protocol_version`, `request_id` | `uint32`, `uint64` |
| `method`, `assurance_level`, `meta`, `entries`, `compare` | forwarded unchanged |

| Engine response | Contents |
| --- | --- |
| `Pass` | Every check passed and every compared pair cleared the level. |
| `Failed` | `code`, `validation_reason` |
| `ProtocolFailure` | malformed, too large, unsupported version, unknown method or level |

Every variant MAY carry a `debug_report`. The Engine returns no scores.

#### 3.3.1 Isolation

The Engine sees raw biometrics and may run closed or injected models. The Verifier MUST confine it:

- no network access, no writable persistent storage, no process creation;
- no access to the Verifier's signing or encryption keys;
- a single channel, the Engine request and response, both bounded in size;
- a crash, timeout or protocol violation ends in `internal`, never in a token.

Consequences:

- The Engine only decides `Pass` or `Failed`. The Verifier computes every claim, so a compromised Engine can cause false accepts or rejects but cannot forge claims.
- `debug_report` is the Engine's only data path out, and reaches only the requester inside the padded, encrypted response. Timing remains a side channel.

The reference implementation uses Minijail with a separate user and namespaces, a kill-on-violation seccomp policy (no sockets, processes, mounts or `ptrace`), a read-only root, no writable-and-executable memory, and a Unix socket pair.

### 3.4 Token

- The token is a CWT in an untagged `COSE_Sign1`, encoded as deterministic CBOR.
- The protected header MUST contain `alg` (label `1`) = `-65537` (Private Use, `BabyJubJub-EdDSA-Poseidon2`) and `kid` (label `4`) = the compressed signing public key.
- The signature is `BabyJubJub-EdDSA-Poseidon2` as defined in [WIP-100][wip-100], over the [signed digest](#342-signed-digest).

#### 3.4.1 Token claims

| Claim | Key | Encoded key | Value |
| --- | --- | --- | --- |
| `iat` | `6` | `06` | Time of the comparison, seconds since the Unix epoch. |
| `eat_profile` | `265` | `19 01 09` | MUST be `https://world.org/eat/evt/v1` ([RFC 9711](https://datatracker.ietf.org/doc/rfc9711/) §4.3.2). |
| `method` | `-90000` | `3a 00 01 5f 8f` | `method` from the request. |
| `assurance_level` | `-90001` | `3a 00 01 5f 90` | `assurance_level` from the request. |
| `request_hash` | `-90002` | `3a 00 01 5f 91` | `R(SHA-256(plaintext))` over the exact bytes the Verifier decrypted. |
| `compared_entry_hash_k`, `k = 0..3` | `-90003 - k` | `3a 00 01 5f 92` … `95` | `R(SHA-256(entries[compare[k]].data))`, or `0` if `compare` has no position `k`. |

- Claims MUST appear in the order above, the bytewise order of their encoded keys ([RFC 8949 §4.2.1](https://datatracker.ietf.org/doc/html/rfc8949#section-4.2.1)).
- `iat`, `method` and `assurance_level` are CBOR uints; `iat` is a 4-byte uint (`0x1a`) as in [RFC 8392](https://datatracker.ietf.org/doc/html/rfc8392#section-3.1.6).
- Hash claims are 32-byte byte strings, the canonical big-endian encoding of a field element.
- `R(d)` reads the 256-bit digest `d` as a big-endian integer modulo $p$, equivalently `hi * 2^128 + lo` in $F$ for its 128-bit halves.
- `eat_profile` is not in the signed digest and MUST be treated as an untrusted hint.
- `iat` MUST be the time of this comparison, from the Verifier's clock. The Verifier MUST NOT reuse an earlier `iat` or take it from the request.
- Entry hashes MUST be over the received bytes, never over an extracted embedding.
- The Authenticator computes `request_hash` over the plaintext before encrypting it, so both sides hash identical bytes.
- `meta` and entries not in `compare` are bound only through `request_hash`.
- The token carries no similarity scores.

#### 3.4.2 Signed digest

The signed message is **not** the COSE `Sig_structure`. It MUST be output element `1` of the Poseidon2 `t=16` permutation over:

```
[DS_EVT_V1, iat, method, assurance_level, request_hash,
 compared_entry_hash_0, compared_entry_hash_1, compared_entry_hash_2, compared_entry_hash_3,
 0, 0, 0, 0, 0, 0, 0]
```

| Constant | Value |
| --- | --- |
| `DS_EVT_V1` | `453338657364062333832270442605270577` (`b"WORLD_ID_EVT_V1"`) |

Seven slots remain free for later claims.

> TODO: the claims needed to identify the Verifier for key attestation.

### 3.5 Verifiability and reproducible builds

OCI verification, reproducible builds. Models may be injected at runtime; [§3.3.1](#331-isolation) keeps them from exfiltrating information.

### 3.6 Key Registry

> TODO

## 4. Rationale

- **Commitments over received bytes.** A hash of an extracted embedding is a value no other party ever signed, so nothing could be bound to it.
- **The Verifier interprets nothing.** Roles, frame semantics and thresholds live in the Engine and the consumer. A new capture method or credential format never changes the request or token.
- **Request hash plus direct claims.** `request_hash` can be compared with a hash an Authenticator signed over the same request, proving the device vouched for exactly the compared input. Claims a consumer reads are signed directly, so it never opens a commitment. It need not check them against `request_hash`: the Verifier is trusted to derive both from one request, as it is trusted for the comparison.
- **Raw `SHA-256` entry hashes.** Issuers already sign `SHA-256` digests, e.g. in a PCP `hashes.json`, so a consumer binds to them without computing `SHA-256`.
- **Field-element digest.** A consumer verifies the token without parsing CBOR. Claims stay unpacked: `t=16` costs about 865 ACIR opcodes against 505 for `t=8`, and leaves room for later claims.
- **`iat` rather than `exp`.** An RP needs the comparison's age. Without it, one successful comparison is replayable forever. The maximum age is the RP's choice.
- **Assurance levels instead of thresholds.** A score's meaning depends on the model and the pair, so an RP cannot pick a meaningful number. An ordered level stays stable across model upgrades.
- **No scores.** Similarity scores are biometric signals; keeping them out of the token keeps them out of every proof.

## 5. Security

> TODO. At minimum: what the attestation covers, what a compromised signing key can forge, model substitution, and the fact that this spec does not improve the trust assumption for the `live` image, which still rests on Authenticator integrity.

## 6. Backwards Compatibility

This WIP introduces a new interface; no existing contracts or functionality are affected.

[wip-100]: https://github.com/worldcoin/world-id-protocol/pull/888
[wip-111]: wip-111.md
