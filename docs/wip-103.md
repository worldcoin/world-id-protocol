---
wip: 103
title: Ownership Proof
description: Mechanism to privately prove ownership of a registered leaf via a blinded leaf-index commitment.
author: Paolo D'Amico (@paolodamico), Daniel Kales (@dkales)
status: Draft
type: Standards Track
created: 2026-03-26
---

## Abstract



## Motivation

There are use cases that reasonably demand users to prove ownership over their World ID using a specific blinding commitment. At the time of writing there are two well known use cases: authentication with issuers, and authentication to generate `session_id_r_seed`s.

### Issuer Authentication

Some examples of use cases which warrant issuers to request authentication from users:
- **Re-issuance**. Allowing the user to get a new copy of their credential due to loss.
- **Status information**. Providing the user with information on their current enrollment state.
- **Cancellation/deletion/unregistration**. Allowing the user to unregister, delete or cancel their existing credential.

For the cases above, issuers need to know the legitimate owner of a Credential is requesting the operation. Issuers do not know the user's `leafIndex` by design, so a mechanism to authenticate based on the Credential's subject (`sub`) must be devised.

### Generation of Session ID _r_ Seeds

The `session_id_r_seed` value is used in the Uniqueness Proof circuit to compute the `sessionId`. This value should generally be preserved such that sessions can be recomputed and verified in Session Proofs. To accomplish that, the `r` value can be generated with OPRF Nodes. In order for OPRF Nodes to know they are generating a seed for a legitimate `leafIndex`, the same Ownership can be used.

## Specification

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in [RFC 2119](https://www.ietf.org/rfc/rfc2119.txt).

This specification introduces a new circuit to the Protocol: **"Ownership Proof"**. The Ownership Proof is a new circuit which allows users to prove _ownership_ (as defined below) using a specific querying input.

At a high level, the circuit MUST perform the following validations:

1. Prove knowledge of the secret key (`sk`) matching -> public key (`pk`)
2. Prove membership of `pk` in the list of authenticator public keys `pk[]`
3. Prove commitment of `pk[]` for authenticator public keys
4. Prove commitment of `pk[]` matches the leaf index in the Merkle Tree
5. Prove `H(domainSeparator, leafIndex, query) == expected_commitment`

where the following are public inputs:
1. `expected_commitment`: the expected output of the hashed commitment (e.g. the `sub` of a credential).
2. `merkle_root`: the root hash of the Merkle tree used for inclusion (representing the `WorldIDRegistry`).
3. `depth`: the depth of the Merkle tree used for inclusion.

In addition to the above:
1. Authenticators MUST restrict Ownersip Proofs subject only to the use cases outlined in this spec or a future spec which references this one.
2. Authenticators MUST NOT allow computations with domain separators not outlined in this spec or a future spec which references this one.

### Use Cases

The list of current allowed use cases is found below:

| Use Case                      | Domain Separator             | Query                                                 | Conditions                                                                                                                                                                    |
|-------------------------------|------------------------------|-------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Proof of `sub` ownership      | `H_CS(id, r)`                | The credential's `blindingFactor`                     | Can only be generated to be used with the Issuer who initially issued the Credential. This is known from the blinding factor used which is generated for a particular issuer. |
| Computing `session_id_r_seed` | `world_id:session_id_r_seed` | A random field element generated by the Authenticator | Only generated for RP requests requiring such computation (i.e. initiating or verifying a session).                                                                           |

// TODO: Document conditionals with more precise definitions.

## Rationale


## Reference Verification

An Issuer will be able to know a specific user is the legitimate owner of a `sub` by performing the following validations:
1. Ensuring the received Ownership Proof ZKP verifies correctly.
2. The root hash (public input) matches a root hash for a valid Merkle tree of the `WorldIDRegistry`, as well as the expected depth.
3. The `expected_commitment` public input matches the expected `sub` being verified. This value may also be used for lookups when the `sub` is not known in advance.

## Security



## Backwards Compatibility

// Existing sessionIds will have to be migrated or revoked.
